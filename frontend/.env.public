# Loaded by `vite build --mode public` (see package.json "build:public").
# Selects the static-snapshot transport: the built bundle reads /snapshot/*.json and
# never calls /api/*. Using a mode file rather than an inline env assignment keeps the
# script identical on Windows and POSIX shells.
VITE_DATA_MODE=snapshot
